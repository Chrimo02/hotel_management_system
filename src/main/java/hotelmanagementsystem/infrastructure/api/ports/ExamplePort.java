package hotelmanagementsystem.infrastructure.api.ports;

public interface ExamplePort {

    // Ein Port ist ein Interface, welches von einem Adapter implementiert wird

    // Das Port-Interface legt fest welche Methoden vom Adapter implementiert werden müssen.
    // Damit wird sichergestellt, dass alle eingehenden API-requests den korrekten Methodenaufrufen in der Domain zugeordnet werden.

    // Ein Adapter arbeitet sowohl mit den Domain-Varianten, als auch mit den DTO-Varianten unserer Objekte

}
